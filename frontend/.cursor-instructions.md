# Cursor AI Assistant Instructions for Anthill Frontend

## 🚨 CRITICAL REQUIREMENTS

**You MUST follow these rules when working with Svelte/SvelteKit code:**

### 1. Always Use Svelte MCP First
- **BEFORE any Svelte code changes**, call `mcp_svelte_list-sections`
- **THEN** call `mcp_svelte_get-documentation` with relevant sections
- **NEVER implement** without checking current documentation

### 2. Svelte 5 Runes Only
- ✅ **USE**: `$state`, `$derived`, `$effect`, `$props`, `$bindable`
- ❌ **NEVER USE**: `writable()`, `$store`, `$:`, `export let`, `<slot />`

### 3. Code Validation
- After implementation, **ALWAYS** run `mcp_svelte_svelte-autofixer`
- Check with `bun run build` and `bun run lint`

### 4. Documentation References
- Read `.svelte-instructions.md` for complete guidelines
- Check README.md for project-specific rules

### 5. Migration Checklist
When modifying existing code:
- [ ] Replace stores with `$state`
- [ ] Replace `$store` with direct access
- [ ] Replace `$:` with `$effect`/`$derived`
- [ ] Replace `export let` with `$props`
- [ ] Replace `<slot />` with `{@render ...}`

---

**FAILURE TO FOLLOW THESE RULES WILL RESULT IN INCORRECT CODE**
